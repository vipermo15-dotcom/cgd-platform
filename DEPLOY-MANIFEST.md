# 배포 매니페스트 — 한 번에 배포 (크레딧 절약용)

> 기준 커밋: **6b994c5** (origin/main 최신) · 갱신 2026-08-04
> 목적: Manus 크레딧을 아껴 **모든 미배포 변경을 1회 배포로 반영**. 아래 체크리스트로 누락 방지.

## ⭐⭐⭐ 이번 배포 최신 추가분 (진도지도 시스템 B시안 인터페이스 리디자인)
- [ ] **좌측 아이콘 레일 전면 교체** — 어두운 240px 사이드바 폐지 → 96px 라이트 아이콘 레일, 역할별 메뉴 15~16개 → 3~4개 상위 그룹 + 서브탭(칩)
- [ ] **색상 테마 교체** — 블루 계열 → 핑크 액센트(#ff385c) 기반 라이트 팔레트
- [ ] **40칸 진도 그리드 신규** — 학생 "내 진도"(`/student/career-progress`) 화면에 추가, 체크리스트 완료율을 40주 타임라인에 근사 표시, 칸 클릭 시 우측 패널에서 해당 구간 준비 항목 확인
- [ ] 학생 홈 화면 재구성 — 인사말+이번 주 상태 → 전체 진도 카드 → 진도 그리드(축소판) → 이번 주 할 일/상담 2단
- [ ] 학과장·관리자 홈/교육생/진도 현황 화면 여백·통계 카드·숫자 단위(명/건/%) 정리
- [ ] 🔴 **교수 계정 접근 불가 버그 수정** — 기존 메뉴가 관리자 전용 라우트(`/admin`, `/admin/users` 등)를 가리켜 교수 계정에서 열리지 않던 문제. 전용 페이지(`/professor`, `/professor/students`, `/professor/stats`, `/professor/documents`)로 재연결 — 이 화면들은 이번 배포 전까지 메뉴 어디서도 접근 불가능했음
- [ ] 🔴 **훈련센터 메뉴 누락 복구** — "AI 기업-학생 매칭"(`/training/matching`)이 메뉴에 없었음
- [ ] 🔴 **학과장 대시보드 필터 이동 오류 수정** — 존재하지 않는 `/professor/dashboard`로 이동하던 것을 `/professor`로 정정
- [ ] 모바일 하단 탭바 학생 전용 → 전 역할(교육생·학과장·교수·훈련센터·업체) 확장, 진도 그리드/헤더 반응형 보정

## ⭐ 이전 추가분 (진로 매칭·포트폴리오 랜딩페이지)
- [ ] **진로 매칭자료** 기능 — 학과장이 이력서·자소서·희망 취업처 링크를 묶어 학생별로 등록, 학생↔학과장 댓글 소통 (`career_matching_records`/`career_matching_comments` 신규 테이블)
- [ ] **포트폴리오 랜딩페이지 URL 등록**(`savePortfolioLink`) — 학생 진로 진행 현황 화면에 랜딩페이지 링크 노출 (DB에는 이미 5명분 데이터 존재)
- [ ] **사전설문 항목 확장** — 희망연봉·희망지역·취업가능시기 필드 추가
- [ ] 로그인 시 Google 이름으로 기존 커스텀 이름 덮어쓰지 않도록 수정
- [ ] 🔴 학습 자료 허브 크래시 수정, ① 교육생 사이드바 접이식 그룹화, ② 학과장 면담 통합 뷰, ④ 학생 대시보드 '다음 할 일' 카드 (※ ①②④는 이번 리디자인으로 레일/그룹 구조 자체가 바뀌면서 대부분 대체됨)

---

## 1) Manus에 보낼 배포 프롬프트 (복붙)

```
GitHub 저장소 vipermo15-dotcom/cgd-platform 의 main 브랜치 최신 코드(커밋 6b994c5)를
전부 가져와서 프로젝트에 반영하고, 빌드·배포해줘. DB 마이그레이션은 없어(순수 프론트엔드 UI 변경).

[이번 배포 내용 — 인터페이스 전면 리디자인]

기존 어두운 사이드바 + 평면 메뉴 구조를 라이트 테마의 좌측 아이콘 레일 구조로 전면 교체했어.

1. 공통 셸
   - 좌측 사이드바(어두운 240px)를 96px 라이트 아이콘 레일로 교체
   - 역할별 메뉴를 15~16개 평면 목록 → 3~4개 상위 그룹 + 서브탭(칩) 구조로 재편
   - 색상 테마를 블루 계열에서 핑크 액센트(#ff385c) 기반 라이트 팔레트로 전면 교체
   - 모바일: 하단 고정 탭바를 학생 전용에서 전 역할(교육생·학과장·교수·훈련센터·업체)로 확장

2. 교육생 화면
   - 홈: 인사말 + 이번 주 상태 → 전체 진도 카드(진행률 %) → 40칸 진도 그리드(축소판) → 이번 주 할 일 / 상담 2단 구성
   - 내 진도(/student/career-progress): 40칸 진도 그리드 신규 추가 — 칸 클릭 시 우측 패널에서 해당 구간 준비 항목 표시. 기존 5개 탭(체크리스트·워크플로우·AI추천·프리랜서·매칭자료)은 그대로 유지됨
   - 포트폴리오, 상담(AI 에이전트): 레이아웃 여백 정리

3. 학과장/관리자 화면
   - 홈, 교육생(회원관리), 진도 현황 화면 여백·통계 카드·숫자 단위 표기 정리

4. 교수·훈련센터·업체 화면
   - 버그 수정: 교수 계정 메뉴가 관리자 전용 페이지(/admin, /admin/users 등)를 가리키고 있어 클릭해도 열리지 않던 문제를 발견해, 교수 전용 페이지(/professor, /professor/students, /professor/stats, /professor/documents)로 재연결했어. 이 화면들은 이번 배포 전까지 메뉴 어디에서도 접근할 수 없었던 상태였어.
   - 버그 수정: 훈련센터 계정에서 누락돼 있던 "AI 기업-학생 매칭"(/training/matching) 메뉴 복구
   - 버그 수정: 학과장 대시보드의 상태 필터가 존재하지 않는 /professor/dashboard 경로로 이동하던 오류 수정 (/professor로 정정)

[검증 완료 상태]
- tsc --noEmit 0 errors
- vitest run 36/36 통과
- vite build 성공

[빌드/배포]
pnpm install → pnpm build → 배포. 배포 후 아래 계정별 확인을 진행해줘.

[배포 후 확인 체크리스트]
- 학생 계정: 좌측(모바일은 하단) 메뉴가 홈·내 진도·포트폴리오·상담 4개로 보이는지, 내 진도 화면에서 40칸 그리드가 보이고 칸을 누르면 우측 패널이 뜨는지
- 학과장/관리자 계정: 메뉴가 홈·교육생·진도 현황·취업·업체 4개로 보이는지, 각 메뉴의 서브탭(칩)에서 기존 페이지(공고 승인, AI 로그, 진로지도 카드 등)에 모두 접근되는지
- 교수 계정으로 로그인해서 홈 화면이 정상적으로 뜨는지 반드시 확인 (기존에는 메뉴를 눌러도 다른 화면으로 튕기던 상태였을 가능성이 있음)
- 공동훈련센터 계정: "기관 현황" 메뉴 안에 AI 기업-학생 매칭이 보이는지
- 협력기업 계정: 채용공고·실습생 2개 메뉴로 보이는지
- 모바일 브라우저에서 하단 탭바가 5개 역할 모두 정상 표시되는지
```

> Manus가 "저장소 접근 불가"라고 하면 → GitHub에서 저장소를 잠깐 **public** 전환 후 재시도, 끝나면 **private** 복귀.

---

## 2) 참고
- 코드 검증 완료 상태: `tsc` 0 errors · `vitest` 36/36 · `build` 성공 (커밋 6b994c5 기준)
- 40칸 진도 그리드는 **실제 주차별 과제·출석 DB가 없어** 체크리스트 완료율을 40주 타임라인에 근사 표시하는 방식입니다. 실제 주차별 데이터 연동은 별도 백엔드 작업(신규 테이블+라우터+교수용 입력 화면)이 필요하며 아직 미구현입니다.
- 상담(AI 취업진로 에이전트, 14탭)과 학과장 회원관리(일괄매칭 등 운영 도구) 화면은 기능이 복잡해 이번 리디자인에서 색상 체계 전면 개편은 보류하고 레이아웃 여백만 정리했습니다.
