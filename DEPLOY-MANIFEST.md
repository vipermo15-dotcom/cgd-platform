# 배포 매니페스트 — 한 번에 배포 (크레딧 절약용)

> 기준 커밋: **1f40a37** (origin/main 최신) · 작성 2026-07-02
> 목적: Manus 크레딧을 아껴 **모든 미배포 변경을 1회 배포로 반영**. 아래 체크리스트로 누락 방지.

---

## 1) Manus에 보낼 배포 프롬프트 (복붙)

```
GitHub 저장소 vipermo15-dotcom/cgd-platform 의 main 브랜치 최신 코드(커밋 1f40a37)를
전부 가져와서 프로젝트에 반영하고, DB 마이그레이션 적용 후 빌드·배포해줘.

[DB 마이그레이션]
drizzle/ 폴더의 0007~0010 마이그레이션을 순서대로 적용해줘 (pnpm drizzle-kit migrate).
- 0007: 인덱스 추가 + bookmarks.type 컬럼 + 백필
- 0008: job_coaching_requests 테이블(채용공고 첨삭)
- 0009: 사전설문 데이터 컬럼
- 0010: platform_feedback 테이블(플랫폼 피드백 설문)
※ 이미 적용된 마이그레이션에서 "already exists" 오류가 나면 그 항목만 건너뛰고 계속 진행해줘.

[빌드/배포]
pnpm install → pnpm build → 배포. 배포 후 앱이 정상 부팅되는지 확인.
```

> Manus가 "저장소 접근 불가"라고 하면 → GitHub에서 저장소를 잠깐 **public** 전환 후 재시도, 끝나면 **private** 복귀.

---

## 2) 이번 배포에 포함되는 변경 — 누락 방지 체크리스트

### 🎓 교육생(학생) 기능
- [ ] AI 취업진로 에이전트 `/student/ai-agents` — 14개 탭
      (사전설문·진로상담·내진로카드·기업추천·포폴전략·포폴코치·포폴점수·자기소개서·면접준비·학습로드맵·취업준비도·주간리포트·공고분석·포폴가이드)
- [ ] **AI 에이전트 탭 전환 시 입력·결과 유지**(초기화 방지) ← 피드백 최다 요청
- [ ] 희망기업 매칭 `/student/job-matching`
- [ ] 채용공고 첨삭 `/student/job-coaching`
- [ ] 학습 자료 허브 `/learning-hub`
- [ ] 플랫폼 피드백 설문 `/feedback`
- [ ] 내 프로필에서 **이름 직접 수정**
- [ ] 카톡 인앱브라우저 로그인 안내(외부 브라우저 열기) + 모바일 최적화

### 🧑‍🏫 학과장/관리자/공동훈련센터 기능
- [ ] **AI 자동 매칭** `/admin/ai-matching` (학생 데이터 자동분석→실제공고 매칭→진로카드 저장+알림+대신지원)
- [ ] **사후지도** `/admin/follow-up` (취업확정 학생 D+30/60/90 점검·메모) ← 신규
- [ ] 채용공고 첨삭 검토 `/admin/job-coaching` + 요청 시 검토자 알림
- [ ] 취업 준비율 `/admin/student-readiness`
- [ ] 회원관리: **이름 인라인 수정** + **교육생 서류 수정**(이력서·자소서·포트폴리오) 모달
- [ ] 진로지도 사전설문 결과 조회 + 첨삭 적용
- [ ] 피드백 결과 보기 `/admin/feedback-results`
- [ ] 대시보드 카드 정합성(전체 회원·AI 분석·취업 확정 수치)
- [ ] professor 역할 = admin 동일 권한(윤한정 교수님 포함)

### 🐛 버그 수정 (중요)
- [ ] AI 자동 매칭 크래시(`items.slice(...).map`) 수정 — sanitizeList 방어
- [ ] AI 에이전트 LLM 응답 처리 버그(빈 결과 반환) 수정
- [ ] 빌드 정상화(feedback/ai-agent/dashboard 컴파일 오류)
- [ ] 최종합격 시 취업 축하 배너 자동 생성
- [ ] publicSlug 빈 문자열 → null (UNIQUE 위반 방지)

---

## 3) 배포 후 검증 체크리스트 (계정별)

**학생 계정**
- [ ] 사이드바에 AI 취업진로 에이전트·희망기업 매칭·채용공고 첨삭·학습 자료 허브 메뉴 노출
- [ ] AI 에이전트에서 입력 후 **다른 탭 갔다 와도 값이 유지**되는지
- [ ] 내 프로필에서 이름 수정 저장되는지

**학과장/관리자 계정**
- [ ] 사이드바에 **사후지도**·AI 자동 매칭·채용공고 첨삭·취업 준비율·피드백 결과 보기 노출
- [ ] AI 자동 매칭 실행 시 결과가 뜨고 진로지도 카드에 저장되는지(크래시 없음)
- [ ] 사후지도에서 취업확정 학생 D+30/60/90 체크·저장되는지
- [ ] 회원관리에서 이름 연필 수정·서류 수정 모달 동작하는지

---

## 4) 참고
- 코드 검증 완료 상태: `tsc` 0 errors · `vitest` 36/36 · `build` 성공 (커밋 1f40a37 기준)
- 아직 **미구현**(다음 기회): AI 에이전트 결과 **영구 DB 저장/이력**(현재는 세션 내 유지까지), 희망기업 매칭 고도화
